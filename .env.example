# Server
PORT=3000
BASE_URL=http://localhost:3000

# Gmail SMTP (use App Password if 2FA enabled)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=yourgmail@gmail.com
SMTP_PASS=your_app_password

# Optional throttling (ms delay between emails)
SEND_DELAY_MS=400
MAX_PER_BATCH=200